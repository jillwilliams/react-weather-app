import React, { Component } from 'react';

const Title = () => (
  <div>
    <h1>Welcome to Weather App</h1>
    <p>Find out local temperature, weather conditions, and more!</p>
  </div>
);

export default Title;
